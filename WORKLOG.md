# WORKLOG — Trip Canvas

세션/에이전트 간 핸드오프 로그. **"다음 할 일"은 여기 쓰지 않는다 → `NEXT.md`.**
전체 작업 목록·완료 기준은 `tasks.md`가 원본. 여기는 결과 요약만 append.

## Current State

- Status: 배포됨·운영 검증 완료 — https://vacatioon-planner.vercel.app
- Focus: 다음 할 일은 `NEXT.md`가 단일 출처. 지금은 **T10-5·47·46·48 이 작업 트리에 미커밋**
- 검증 기준선: vitest **536**(`--no-file-parallelism`) · pgTAP **209** · **E2E 4 통과** · tsc 0 · lint 0
  (2026-08-20 실측 — 미커밋분 포함. dev 서버를 켜 두면 SC-008 이 경합으로 깨진다)
- 원격: `origin` = github.com/kimsangchan/vacatioon_planner — `master` 동기화됨
  (2026-08-11 의 "원격 없음·백업 0" 경고는 해소됨)
- Last updated: 2026-08-20 (길찾기 NCP · 확정 · 별표+공유뷰 · TDS 적용)

## History (append; 최신이 위)

- 2026-08-20 (마무리) — **E2E 를 되살리고, 지도에서 순서와 경로를 읽게 했다** (결정 #49).
  ① **E2E 2건이 깨져 있었고 원인이 둘로 갈렸다.** (a) SC-004(390×844): 캔버스를 뷰포트 높이에 고정하면서
  (스크롤바가 지도 폭 15px 을 먹던 결함을 고치며) **닫힌 시트의 손잡이가 화면 밖**이 됐다 — 예전엔 페이지가
  세로로 스크롤돼 Playwright 가 스크롤해 눌렀을 뿐이다. 시트를 여는 문은 결정 #42 이후 하단 메뉴이므로
  테스트를 그 경로로 고쳤다. (b) journey: 데스크톱에서 상세가 패널 안으로 들어가며 목록이 자리를 내주는데
  테스트가 목록 버튼을 눌렀고, **다른 에이전트가 행 액션을 '작업 열기' 뒤로 접은 것**까지 겹쳐 있었다
  (유닛 테스트만 고쳐져 있었다). 상세를 닫고, 작업을 펴고 누르도록 고쳤다 → **4/4 통과**.
  ② **"이동시간을 왜 내가 하나씩 넣냐"는 지적** — 실은 자동으로 나온다(#45). 그 일차에 방문이 하나뿐이라
  이을 구간이 없었을 뿐인데 **화면이 아무 말도 안 했다**. "한 곳 더 담으면 알려드려요"를 붙이고,
  '이동 적기' 아래에 "예매한 기차·버스·비행기를 적는 자리"라고 밝혔다 — 그 버튼이 이동시간의 문처럼 보였다.
  ③ **핀 숫자가 낭비되고 있었다** — 색도 일차, 숫자도 일차라 같은 말을 두 번 했고 한 일차의 핀이 전부 같은
  숫자였다. 숫자를 **그 날 몇 번째 방문**으로 돌려 채널을 갈랐다(색=어느 날 · 숫자=몇 번째).
  ④ **그 날 달리는 길을 일차 색 선으로 그린다.** NCP 응답의 `path` 좌표를 버리고 있었다.
  직선으로 이으면 제주에서 바다를 가로지른다. 경로는 타임라인이 받아 지도에 **알리기만** 한다 —
  시간 표시는 타임라인의 일, 그리기는 지도의 일이라 상태를 두 곳이 들면 어긋난다.
  실브라우저 확인: 핀 1·2·3 · 선 `stroke rgb(3,105,161)` = `--day-sky` · 이동시간 2구간.
  ⑤ 남은 폼(로그인·달력·LegForm·ManualPlaceForm·TripTitleField·ConfirmRow)도 TDS text-field 규격으로.
  하드코딩 알파 0건, `dark:` 3개만 남았다.
  **원인 확정 하나**: 통합 테스트가 산발적으로 깨지던 것은 dev 서버가 아니라 **vitest 자신의 파일 병렬
  실행**이 로컬 Supabase 하나를 두고 다투는 것이었다 — `--no-file-parallelism` 이면 536/536.
  `supabase/CLAUDE.md`·`AGENTS.md` 에 적어 뒀다.

- 2026-08-20 (이어서) — **별표 협의를 끝까지 잇고, TDS 를 실제로 입혔다** (결정 #46·#48).
  ① **공유 뷰 `/s/[token]` 신설** — 이전 세션까지 "RPC 는 있는데 화면이 없다"였던 것을 닫았다.
  헤더 '같이 보기'로 링크를 만들고, 받은 사람은 **로그인 없이** 일정을 보고 별표만 남긴다.
  표는 `get_shared_votes(token, voter_key)`(`0012`)가 장소별로 접어 준다 — `get_shared_trip`(100줄)을
  재정의하지 않은 이유는 여행 전체 계약이 걸린 함수를 표 하나 때문에 다시 쓸 이유가 없어서다.
  **voter_key 를 내보내지 않는다**: 남의 키가 나가면 그 키로 남의 표를 덮을 수 있어, "내 표"는
  호출자가 자기 키를 들고 와 서버가 대조한다. 캔버스를 재사용하지 않고 읽기 전용 화면을 따로 만든 것도
  같은 이유다 — 프롭을 하나씩 끄다 빠뜨리면 남이 남의 여행을 고친다. **실브라우저로 왕복 확인**:
  링크 생성 → 열기 → 별 2점 → DB 저장.
  ② **TDS 를 22개 파일 200여 곳에 적용**. 강조 CTA 를 검정에서 브랜드 블루로, 검색은 TDS text-field
  (48/12/surface-2/포커스 1.5px), 탭은 알약에서 **언더라인**으로, 버튼 사다리(XL56·L48·M40·S32),
  타임라인 행은 2줄 list-row 로, ⋯·↩·↑↓ 이모지 글립을 SVG 아이콘으로. `dark:` 200여 개를 지웠다 —
  토큰이 이미 테마를 안다. 일차 색 팔레트 8개를 늘 펼쳐 두던 것도 칩 하나로 접었다.
  ③ **레이아웃 결함 하나를 실측으로 잡았다**: 지도가 화면 오른쪽을 못 채우던 원인은 세로 스크롤바였다
  (body 1166px vs 뷰포트 860px → 스크롤바가 폭 15px 을 먹음). 트립 페이지를 `h-dvh overflow-hidden`
  으로 묶어 해결. **유닛 테스트로는 절대 안 나오는 종류다.**
  ④ **PC 는 장소 상세를 지도 위 카드가 아니라 패널 안으로** 옮겼다(사용자 요청, 네이버 지도 방식).
  카드가 커지면 지도 위에서 스크롤이 생겨 정작 지도를 가린다. 모바일은 핀에 붙는 카드 그대로다.
  두 벌 렌더 대신 `matchMedia` 로 하나만 고른다 — id 중복 함정(#42)을 이미 겪었다.
  ⑤ 하단탭이 지도 조작·카드 열림 중 물러나고, PC 좌측 패널은 접힌다.
  **아직 안 본 것**: 모바일 실화면, E2E 4건 재실행. 로그인·달력·LegForm 은 색만 바뀌고 구조는 그대로다.
  **또 물린 것**: dev 서버를 켜 둔 채 vitest 를 돌리면 SC-008 쿼터 테스트가 로컬 Supabase 경합으로
  깨진다(단독 7/7). 그리고 `db reset` 은 정말로 다 지운다 — 백업해 두고 세 번 되살렸다.

- 2026-08-20 — **길찾기를 NCP 로 되돌리고, 협의·확정·디자인을 얹었다** (결정 #45~#48).
  ① **T10-5 를 카카오모빌리티 → NCP Directions 15 로 전환.** 인계 문서에 적힌 기각 사유
  ("NCP 무료 이용량에 Directions 없음")가 **사실이 아니었다** — 사용자 콘솔에 Directions 5 월 60,000회 ·
  15 월 3,000회 무료가 잡혀 있었고, 기존 NCP 키로 바로 200 이 떨어졌다. 같은 좌표로 양쪽을 실제로 불러
  비교하니(제주 공항→중문→성산) 53분+90분 vs 53분+93분으로 차이가 없고 NCP 만 유류비를 준다.
  **교훈**: 근거를 NEXT.md 에만 적으면 개정 때 통째로 사라진다 — 되찾고 보니 그 근거 자체가 틀려 있었다.
  이제 근거는 decision-log 로 가고 **검증 날짜와 방법을 함께** 적는다(NEXT.md 규칙에 박아 뒀다).
  전환에서 손본 것: GET · 헤더 인증 · `summary.waypoints[]`+`goal` 파싱 · **밀리초→초** ·
  `MAX_POINTS` 32→17(Directions 5 는 경유지 6개에서 `400 too many waypoints`).
  ② **확정/미확정**(#47, `0011`, 기본값 true) — 경로는 확정된 것만 잇는다. 기본을 false 로 두면
  기존 여행의 경로가 통째로 사라지고 담을 때마다 두 번 눌러야 한다. 별표가 임계값을 넘으면 자동 확정,
  은 하지 않았다: 남이 별을 누를 때마다 경로가 저 혼자 바뀌면 아무도 설명하지 못한다.
  ③ **별표 협의**(#46, `0010`) — `place_votes`(place_id+voter_key PK) + `vote_shared_place`
  SECURITY DEFINER RPC. **계정을 요구하지 않는다** — 결정 #3(읽기전용 공유)을 뒤집지 않고 별표 쓰기만
  RPC 로 뚫었다. 다만 **`/s/[token]` 공유 뷰가 아직 없어**(tasks.md 기준 P2 미구현) "남이 누르는"
  절반은 미완이다. DB·RPC·pgTAP 16 은 준비돼 있다.
  ④ **TDS(토스) 디자인 각색**(#48) — 토큰·Pretendard·탭바·체크박스·패널까지 옮겼다.
  가장 큰 각색은 **색의 역할 분리**: 액션 색은 브랜드 하나, 일차 8색(#41)·카테고리 3색은 데이터 전용.
  TDS 는 라이트 값만 공개돼 있어 **다크 alias 는 이 앱이 정의**했다. 규약은 `docs/design/design-system.md`.
  ⑤ 하단탭이 지도 조작·카드 열림 중에 물러난다(#48). 탭 이모지는 SVG 아이콘으로 바꿨다.
  **아직 안 한 것**: 버튼·입력·카드·리스트의 TDS 적용(T10-14), 공유 뷰(T10-13), **실브라우저 확인**.
  pgTAP 을 고치며 두 번 물렸다 — `on conflict (place_id, …)` 가 함수 인자 이름과 부딪혀 42702 로 죽고
  (제약 이름으로 가리켜 해결), `'Þad…'::bytea` 리터럴이 이스케이프로 먹혀 `decode(…,'hex')` 로 바꿨다.

- 2026-08-20 — **문서 정합화(catch-up 재실행)**. 구조(CLAUDE/AGENTS/NEXT/WORKLOG/훅/스코프)는 이미
  멀쩡했고 **내용이 트리보다 뒤처져 있었다**: NEXT.md 가 T10-9 완료를 빠뜨렸고, T10-5 를 아직
  "[다음]·사용자 작업 선행"으로 적어 둔 채였는데 **실제로는 구현이 작업 트리에 들어와 있었다**
  (`api/directions` · `lib/route/*` · `lib/timeline/route.ts` + 테스트, 미커밋). 새 세션이 그 블록만
  믿으면 있는 코드를 다시 짤 자리였다. 기준선도 세 곳이 서로 달라(NEXT 456 / WORKLOG 419) 실측해
  **490 passed(54 파일)** 로 통일했다.
  **끊긴 근거 하나를 이었다**: 미커밋 코드가 10곳에서 `결정 #45` 를 참조하는데 decision-log 는 #44 에서
  끝나 있었다 — 주석에 흩어져 있던 근거(경유지 1회 호출 · 서버 전용 키 · Leg 구간은 추정 안 함 ·
  React Query 미사용)를 #45 한 줄로 모았다.
  함정 10줄은 매 세션 주입되던 자리에서 **읽힐 자리로** 내렸다 — `src/components/`(jsdom 레이아웃·
  지도 투영·`h-full`·포인터 캡처·id 중복) · `supabase/`(db reset·테스트 경합) · AGENTS.md 전역(build·lockfile).
  `e2e/CLAUDE.md` 는 dev 서버 함정을 이미 더 자세히 갖고 있어 건드리지 않았다.
  주입 컨텍스트 4848B → 2951B.

- 2026-08-19 — **검색 지우기 + 패널 스와이프** (T10-9, 결정 #44). 끌기는 포인터 캡처 없이는
  손잡이 밖으로 나가는 순간 끊긴다 — 유닛 테스트로는 안 보이고 브라우저에서 끌어 봐야 나온다.

- 2026-08-19 — **모바일 재구성** (T10-8, 결정 #42·#43). 하단 메뉴(지도·보관함·일정)로 화면을 가르고,
  검색을 화면 맨 위 상주 바로 빼고, 지도를 전체화면으로 돌렸다. 장소 카드는 일정에 넣고 빼는 자리까지 됐다.
  **실측으로 잡은 것 셋**: ① `h-full` 이 flex 아이템 안에서 퍼센트 기준을 못 잡아 지도가 백지로 떴다
  (두 번 겪고 `absolute inset-0` 로 고침) ② 검색을 모바일·데스크톱 두 벌 두면 `id="place-search"` 가
  문서에 둘 생겨 라벨이 엉뚱한 입력에 붙는다 → 하나만 둔다 ③ 카드가 핀 위 공간보다 크면 화면 위로
  밀려 제목이 잘린다 → 높이를 재는 대신 **쓸 수 있는 공간을 max-height 로 넘긴다**.
  배지가 세로로 접히던 것(`shrink-0` 누락)도 함께 고쳤다.
  원격 Supabase 에 `0008`·`0009` 반영을 덤프로 확인하고 커밋·푸시했다.

- 2026-08-18 — **사용자 피드백 4건 배포 완료** (T10-1·2·3·4, 결정 #39·#40·#41, `3a88c8d`).
  원격 Supabase 에 `0008_place_estimated_cost.sql`·`0009_day_color.sql` 을 선적용하고 Vercel Production 배포 성공.
  ① 검색 결과가 잘려 보이던 것: 모바일 시트가 46% 고정인데 결과 도착 시 확장이 없었다.
  ② 장소 카드를 **핀에 붙는 팝업**으로. 처음엔 패널 안에 렌더해 "왼쪽 패널에 여전히 있다"는 지적을 받았고,
  이어 "확대축소를 하든 따라와야" 지적을 받아 `MapProvider.project`·`onViewportChange` 를 신설했다.
  실측으로 잡은 함정: `fromCoordToOffset` 은 화면이 아니라 **월드 픽셀**이라 지도를 끌어도 값이 고정이었다.
  ③ **예상 금액**(`places.estimated_cost`, `0008`) + 경비 요약 — 확정/예상 포함 두 줄, 보관함은 소계만.
  결정 #24 는 뒤집지 않았다(실제 지출은 Stop 에 그대로).
  ④ **일차별 색상**(`days.color`, `0009`) — 사용자가 "카테고리는 아이콘, 일자는 숫자"로 채널을 갈라 줬다.
  색은 일차만, 모양은 카테고리. 핀·타임라인·보관함이 같은 `CATEGORY_ICON_PATH` 를 쓴다.
  **교훈**: 카드가 안 보인다는 신고를 유닛 테스트가 3번 연속 놓쳤다 — jsdom 에 레이아웃이 없어서다.
  단언을 "DOM 에 있다"에서 "리스트 패널 밖에 있다"·"시트가 올라갔다"로 바꾼 뒤에야 빨간불이 났고,
  지도 추종은 결국 **브라우저에서 실제로 끌어 보고서야** 원인이 나왔다.

- 2026-08-12 — **배포 완료·운영 검증** (T9-1). https://vacatioon-planner.vercel.app
  원격 Supabase(스키마 7종 push·RLS 8/8) · Vercel env 7종 · 카카오 로그인 · 실지도 · 실키 검색 전부 운영에서 확인.
  가는 길에 잡은 것: lockfile 리눅스 누락(컨테이너에서 재생성·검증) · CI 의 next typegen 누락 ·
  anon 권한 드리프트(결정 #38) · state 쿠키 Secure 누락 · **검색 캐시가 배열 아닌 값을 응답으로 내보내던 버그**
  (운영에서 200+null → 클라이언트가 네트워크 오류로 오인). CI 전체 초록

- 2026-08-12 — **카카오 로그인 배선 + keepalive** (결정 #33·#34·#35). 카카오는 주 경로로 추가하되
  메일 OTP 는 남겼다(E2E 가 그 경로로 자동화돼 있다). `/auth/callback` 신설, 인증 왕복의 되돌아갈
  주소를 `appRedirectTarget` 한 곳으로 모으며 `/auth/confirm` 의 같은 호스트 버그도 고침.
  무료 티어가 조용히 꺼지는 것(Supabase 1주·GitHub 60일)을 막는 워크플로 추가.
  **카카오 키는 아직 없다** — 콘솔 등록 주소는 앱(3010)이 아니라 Supabase(54321)다

- 2026-08-11 — **낡은 세션이 만들던 막다른 화면 수정** (결정 #30·#31·#32). 사용자가 실제로 막혔다:
  토큰 `iat` 가 미래로 찍혀 요청마다 거부됐는데 쿠키가 남아 307 이 무한 반복 → "눌러도 반응 없음".
  거부된 세션은 쿠키를 지우고 사연과 함께 `/login` 으로 보낸다(불통·`/auth/confirm` 은 예외).
  로그아웃 경로 `/auth/signout` 신설 — 폼 POST 라 JS 없이도·세션이 깨져도 눌린다.
  **울트라코드 검증(조사 3 → 구현 1 → 적대적 5)이 구현의 결함 4건을 잡았다**: signOut 기본 scope 가
  'global' 이라 모든 기기가 로그아웃되던 것 · Route Handler 리다이렉트가 사용자 호스트를 무시하던 것 ·
  `?reason=toString` 500 · 변이 3건을 못 잡던 테스트. 전부 직접 재현 확인 후 수정하고 변이 테스트로 못박음

- 2026-08-11 — **생성 흐름 개편(결정 #27)**: 새 여행이 이름·날짜를 묻지 않는다 — 누르면 초안이 생기고
  곧장 캔버스로. 이름은 헤더 `TripTitleField`, 기간은 `DateRangeCalendar`(범위 선택 달력). `NewTripForm` 삭제.
  **울트라코드 리뷰(4관점×적대적 검증, 에이전트 24)가 심각한 결함 1건을 잡았다**: 달력에서 두 번 눌러
  범위를 잡는 경로가 실제 포인터 순서에서 무너져 **모바일에서 여러 날 여행 생성이 불가능**했다(결정 #28).
  `fireEvent.click` 만 쓰던 테스트가 이를 가렸다 — 실제 이벤트 순서 회귀 2건 + E2E 기간 반영 단언 추가.
  함께 고침: 헤더 편집기 양방향 배타(L-09) · `unknown` 문구가 이름변경/삭제 실패에 "만들지 못했어요"라고
  거짓 안내하던 것 정정

- 2026-08-11 — **T5-3 완료**: `0006_stale_search.sql`의 `get_stale_search`(5분 창 무시·상한 7일) +
  pgTAP 11 RED→GREEN. 구현 중 **기존 `cached[]` 분기가 도달 불가능한 죽은 코드**였음을 발견 —
  502 경로가 직전에 miss를 낸 `get_cached_search`를 다시 부르고 있었다(결정 #26). 문구도 "방금 받아둔"→"전에 받아둔"으로 정정.
  05 계약의 "추가 후 성립" 단서 해소

- 2026-08-11 — **T0-2 완료**: NCP Maps 실키가 3010에서 인증 성공함을 실브라우저로 검증
  (SDK 네임스페이스·타일 56장·fake/실패 배너 0건, 제주 지도+핀 육안 확인). 무료 이용량은 월 600만 건(대표 계정).
  **카카오 폴백 불필요 — 08 잔여우려 1 해소**(결정 #25). `MapProvider` 추상화와 provider enum의 kakao는 유지
- 2026-08-11 — **T8 완료**: E2E 4스펙 green(여정 1 스모크 18.7s). SC 실측 — SC-001 결정 3지점·1106ms·
  확인 대화 0 / SC-002 호버→카드 중앙값 30ms(≤400) / SC-004 390×844 전 Leg 시각 1스크롤 내(681px).
  T8-3 SC-003 체크리스트 5항목 전부 ✓ (강조 CTA는 12개 상태 전수 측정 전부 ≤1, 에러 화면 2종 다음 행동 버튼 확인).
  계측용 임시 스펙 `zz-tmp-recheck.spec.ts`는 실행 후 삭제
- 2026-08-11 — catch-up 컨텍스트 구조 도입: `AGENTS.md` 단일 원본화, 루트 `CLAUDE.md`는 행동규칙만,
  `NEXT.md`/`WORKLOG.md` 분리, 스코프 4종(`src/lib`·`src/components`·`supabase`·`e2e`) CLAUDE.md,
  SessionStart 훅 배선
- 2026-08-07 — `487329e` T7-3: Leg 예매 캡처 첨부·기간 변경(축소 확인)·삭제/되돌리기(Trip 소프트 90일 복구)
  + dev 포트 3010 전환. vitest 280 · pgTAP 136 · 실브라우저 ⓐⓑⓒ 확인
- 2026-08-07 — `1dae68f` T7-1·2: 일정 배치·타임라인(통합 position 병합)·이동 입력·방문 지출.
  Day 지출 합계 = Stop+Leg (결정 #24) · `0005_stop_cost.sql`
- 2026-08-07 — `ce5c76f` T6-B: 사진 미리보기·업로드·수동 등록 + **지도 인증 실패 시 앱 크래시 수정**
  (`navermap_authFailure` 구독, 회귀 3건)
- 2026-08-07 — `002b354` T6-A: 캔버스 코어 — 지도 추상화(`MapProvider`)·검색 저장·리스트↔핀 동기화.
  클러스터링은 보류
- 2026-08-07 — `51eae38` T4 누락분(trips 데이터 계층) · `c1bde13` T5 검색 프록시 ·
  `76455c5` T4 OTP 인증·여행 목록. 카테고리 어휘 실측 정정(결정 #23)
- 2026-08-06 — `9fba3b6` T3: DB 스키마·RLS·RPC 9종 + pgTAP 104어서션 ·
  `76d39a1` T2: 순수 도메인 로직 4종(테스트 우선) · `08eda94` 기반(설계 패키지·SPEC·tasks)
- 2026-08-06 — 설계 패키지 확정(service-autopilot 8단계, GATE 2회 통과) → `docs/design/`.
  T0-4 실측: 검색 API HUB는 WGS84×10⁷ 반환(결정 #20)
