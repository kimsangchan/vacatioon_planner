-- 전화번호를 담는다 (사용자 요청 — "주소랑 영업시간 같은 거 받아서 적어줄 수 있으면").
--
-- **영업시간은 못 담는다**: 네이버 지역검색도 카카오 로컬도 주지 않는다
-- (01-recon — 카카오는 "이미지·영업시간 미제공" 공식 답변). 전화번호는 네이버가 주는데
-- 우리가 프록시에서 버리고 있었다. 버릴 이유가 없어 살린다.
--
-- memo 와 같은 규약: not null + 빈 문자열 기본값. null 과 '' 두 가지 "없음"을 만들지 않는다.

alter table public.places
  add column phone text not null default '';
