# Decision Log — 여행 플래너 "Trip Canvas"

| # | 일자 | 결정 | 선택 이유 | 버린 대안과 이유 |
|---|---|---|---|---|
| 1 | 2026-08-06 | 플랫폼 = 반응형 웹 + PWA (Next.js, Vercel 배포) | 아이폰(여행 중)+데스크톱(계획) 한 코드베이스, iOS 26 홈화면 웹앱 기본 실행, 비용·심사 0. 필요 기능 전부 PWA 범위 내 (01-recon 플랫폼 조사) | 네이티브 iOS: 연 $99+심사+Swift 학습, 계획용 데스크톱 화면 부재. Capacitor 래핑 전제: 현 시점 이득 없음 — 필요해지면 그때 도입해도 손해 없음 |
| 2 | 2026-08-06 | 지도 = 국내 네이버(MVP) → 해외 구글(2단계), MapProvider 추상화 계층 필수 | 사용자 지정(습관·국내 POI·네이버 상세 링크 연결 가치). 지역 검색 API 일 25k 무료 확인 | 구글 단일(추천안): 사용자가 국내 POI 품질·네이버 습관을 우선함. 카카오: 네이버 무료 이용량 조건 미충족 시 폴백으로 유지 (탈락 아닌 대기) |
| 3 | 2026-08-06 | 사용 주체 = 1계정(매직링크) + 여행별 읽기전용 공유 링크 토큰 | 검증된 최소 공유 모델(트리플·Wanderlog), 인증·격리 비용 최소 | 완전 개인용: 동행자 공유 가치 포기 대비 절감 미미. 공개 서비스: 개인정보·인증·비용 과투자 |
| 4 | 2026-08-06 | 장소 등록 = 앱 내 검색(네이버 지역 검색 API 서버 프록시) | 클릭 최소·안정적. 네이버지도 저장목록 공식 연동 부재가 확정 사실 (01-recon 핵심 발견 1) | 공유링크 파싱: 비공식 포맷 의존 취약 → 2단계 후보로 기록. 저장목록 자동 연동: 기술적 불가 |
| 5 | 2026-08-06 | 장소 사진 = 사용자 업로드(클라 리사이즈) + 네이버 상세 링크 보완. 구글 단계에서 해외만 자동 사진 | 국내 검색 API 사진 미제공 확정 (카카오도 동일 — 01-recon 핵심 발견 2). 스크래핑은 약관 리스크로 금지 | 네이버 이미지 검색 API 자동 매칭: 정확도·약관 불확실 → 2단계 검토 후보 |
| 6 | 2026-08-06 | 좌표는 WGS84(lat/lng)로 정규화 저장, KATECH(mapx/mapy)은 입수 시 즉시 변환 | 제공자 교체(구글·카카오)에도 데이터 불변 — MapProvider 추상화의 전제 | 제공자 원시 좌표 저장: 제공자 락인, 이중 표시 버그 원천 |
| 7 | 2026-08-06 | 이동(Leg) = 수동 입력 폼 + 일자 타임라인 표시. 예매처 연동 non-goal | 국내 예매처(코레일·고속버스 등) 공개 API 부재. 수동 입력도 "한눈에 보기" 가치는 충족 | 메일 파싱·예매처 연동: 유지보수 대비 가치 낮음(개인 도구) |
| 8 | 2026-08-06 | 미배정 장소 "보관함" 분리 — 수집(저장)과 배치(일정)를 다른 상태로 모델링 | 도메인 업무 흐름의 1단계(수집)와 2단계(배치)가 실제로 분리돼 있음 (01-recon) | 저장 즉시 날짜 강제 배정: 수집 마찰 증가 — "일단 저장"이 핵심 사용 패턴 |
| 9 | 2026-08-06 | 스택 = Next.js + Tailwind + Supabase(Auth·Postgres·Storage) + Vercel | 1인 개발·무료 티어·지도 JS SDK 호환 fit (01-recon 스택 표) | Firebase: Postgres(관계 모델 — Trip/Day/Stop)가 문서형보다 적합. 로컬 IndexedDB 단독: 공유 링크 불가 |
| 10 | 2026-08-06 | 공동 편집(실시간 협업) 명시적 non-goal | 읽기전용 공유로 동행자 가치 80% 충족, 실시간 충돌 처리는 1인 개발 범위 초과 | Wanderlog식 공동 편집: 차별 요소지만 비용 과다 — 범위 방어 우선 |
| 11 | 2026-08-06 (GATE 1차) | 운영 테이블(search_cache·api_usage) 쓰기 = SECURITY DEFINER RPC — service role 키 런타임 배제 유지 | "RLS 기본 deny + service role 미사용" 원칙과 프록시의 쓰기 필요를 모순 없이 양립 (검토 C-1) | 런타임 service role 허용: 유출 시 전체 데이터 노출 반경. RLS 개방: 클라이언트 카운터 조작 가능 |
| 12 | 2026-08-06 (GATE 1차) | 사진 버킷 = public-read + 무작위 128bit 경로(식별정보 없음), 쓰기만 RLS | 비로그인 공유 뷰·오프라인 캐시·썸네일 프리페치가 서명 URL 만료 없이 성립 (검토 H-4). 신뢰 모델이 공유 토큰과 동일 | 비공개+서명 URL: 익명 공유 뷰어가 URL 생성 불가·1h 만료가 캐시 파괴. 서버 스트리밍 프록시: service role 재도입 + 함수 트래픽 낭비 |
| 13 | 2026-08-06 (GATE 1차) | 인증 = 매직링크 + 6자리 OTP 코드 병행, PWA에서는 코드 입력이 기본 | iOS 설치형 PWA는 Safari와 스토리지 격리 — 링크 탭 시 세션이 Safari에 생기는 함정 (검토 H-7) | 매직링크 단독: 주 사용 채널(PWA)에서 로그인 불능 |
| 14 | 2026-08-06 (GATE 1차) | 백업 = GitHub Actions 일일 pg_dump(30일 보관+월 1회 12개월), RPO 24h | Supabase 무료 플랜 자동 백업 미제공을 공식 문서로 확정 (검토 C-2 — 기존 가정 오류 정정) | Pro 승급($25/월): 무료 티어 원칙 위반. 월 1회 수동 dump: RPO 30일은 여행 기록 소실 허용치 초과 |
| 15 | 2026-08-06 (GATE 1차) | Day 내 Stop·Leg = 통합 position 시퀀스, 시각은 표시 정보(정렬 키 아님) | 무시각 항목과 시각 항목의 상호 순서가 데이터로 표현 가능해짐 — 타임라인 결정성 (검토 H-9) | 시각 우선 정렬+별도 position: 병합 결과 비결정적. 단일 day_items 테이블: Stop·Leg 필드 이질성으로 스키마 오염 |
| 16 | 2026-08-06 (GATE 1차) | provider enum = naver·kakao·google·manual (link nullable) | 카카오 폴백(RB-1)·좌표 직접 등록(FR-016)이 계약 위반 데이터를 만들지 않게 (검토 M-2·H-6) | naver·google 2값: 폴백·수동 등록이 스키마 밖 존재가 됨 |
| 17 | 2026-08-06 (사용자 피드백) | Leg에 가격 필드(`cost_amount` 원 단위 정수) + Day 지출 합계 표시. 정산 계산·분배는 여전히 2단계 | "출발·도착 시간이나 가격 같은 거 넣어서 스케줄이나 나중에 정산할 수 있도록" — 데이터를 지금부터 기록해야 나중에 정산이 가능 | 정산 기능 전체 구현: MVP 범위 초과. 금액 미기록: 나중에 소급 입력 불가능(영수증 유실) |
| 18 | 2026-08-06 (사용자 피드백) | 사진 첨부 대상을 Place + Leg로 확장 (photos.place_id/leg_id 배타, 동일 리사이즈 파이프라인) | 예매 확인·티켓 캡처를 이동 기록에 붙이는 것이 "예매 일정 정보 한눈에" 비전과 직결 | 별도 leg_photos 테이블: 파이프라인 중복. Leg 사진 미지원: 예매 캡처를 메모에 못 담음 |
| 19 | 2026-08-06 (T0 실측) | 검색 API 발급처 = **NCP NAVER API HUB** (`/search/v1/local`, X-NCP-APIGW 키쌍). 검색 결과 5건 고정, 좌표 판별기는 3형식(KATECH·WGS84e7·WGS84 도) 지원 | 개발자센터 신규 등록에 검색 API가 없음을 사용자가 실측 — API HUB 공식 문서로 이관 확인. 콘솔 신청 화면에서 지역 검색 일 25,000회 무료 제공도 사용자 확인 완료 | 개발자센터 경로 고수: 신규 발급 불가라 대안 없음. 프록시 일 12,500 상한은 무료 제공량의 50% 과금 방어선 |
| 20 | 2026-08-06 (T0-4 실호출) | 지역 검색 base URL = `https://naverapihub.apigw.ntruss.com`, endpoint = `GET /search/v1/local`, `display=5`. `mapx/mapy` 실측 형식 = WGS84 × 10^7 정수 (`1263658809`, `348019423` 형태) | `.env`의 NCP NAVER API HUB 키쌍으로 `목포 맛집` 검색 실호출 성공. 응답 필드: `title, link, category, description, telephone, address, roadAddress, mapx, mapy`; HTML 태그 제거 필요성도 재확인 | 기존 후보 base URL `https://naveropenapi.apigw.ntruss.com`는 404. KATECH 고정 변환은 이번 API HUB 응답에는 부적합하므로 기존 3형식 판별기를 유지 |
| 21 | 2026-08-07 (T3 검토 반영) | ① stops의 `(day_id, place_id)` 유니크 제거 — 같은 장소를 하루 2회 배치 허용 ② RLS GRANT 최소화 — anon·운영 테이블 잔여 grant 삭제(접근 시 permission denied) ③ 썸네일 320px `prepareTripPhoto` 추가 ④ pgTAP 매트릭스 22어서션 확장 | ①은 계약에 없던 제품 제약(카페 재방문 등 실사용 차단), ②는 05 권한 모델("share-viewer 테이블 접근 0·운영 테이블 전면 deny")과 문자 그대로 일치시킴, ③은 SPEC(SC-002 프리페치 전제) 누락 보완, ④는 T3-2 완료 기준 충족 | 유니크 유지: 재방문 차단을 정당화할 요구 없음. grant 유지: 정책 실수 시 즉시 노출되는 장전된 총 |
| 22 | 2026-08-07 (T3-3 구현 중 발견) | `get_shared_trip` 실패 카운터(share_fail)는 예외와 함께 롤백됨 — PostgreSQL에 autonomous transaction이 없어 RPC 내부 계측(04 알람 #4)이 트랜잭션 의미론과 충돌. 공유 뷰(P2) 구현 시 "ShareView가 예외 포착 후 별도 트랜잭션에서 카운터 RPC 호출" 방식으로 이행 | 공유 기능 자체가 P2라 지금은 문서화만 — 함수 주석에도 명시됨 | dblink·백그라운드 워커: 개인 도구에 과잉. 계측 포기: 알람 #4(토큰 대입 감지) 무력화 |
| 24 | 2026-08-07 (사용자 피드백) | 지출 기록을 방문(Stop)으로 확장 — `stops.cost_amount`(원 단위 정수, 선택 입력) 추가, Day 지출 합계 = Stop+Leg 합산 | "맛집에도 가격 같은 정보 저장해서 정산할 때 쓸 수 있는 구조" — 장소(Place)가 아닌 Stop에 붙이는 이유: 같은 장소 2회 방문 시 지출이 각각이고, 정산은 실제 방문에 귀속됨(#17과 동일 원칙). 정산 계산·분배 기능 자체는 여전히 2단계 | places.cost_amount: 재방문 지출 표현 불가·보관함 단계의 가격은 확정 전 추정치라 memo가 적절 |
| 23 | 2026-08-07 (T5 구현 중 발견) | ① category 실측 정정 — API HUB의 최상위 토큰은 요리명("한식>칼국수,만두"): 힌트 어휘를 요리 분류로 확대 ② Problem type 3종 신설(auth/required·validation/query-too-short·search/unavailable) ③ 설계 공백: 502 시 `cached[]`는 5분 내 캐시만 가능(get_cached_search가 5분 필터) — stale 제공은 `get_stale_search` RPC 추가 필요(T5-3 백로그) | ①은 실측 데이터(T0-4·스모크)가 SPEC §알고리즘 3의 전제와 다름을 증명 ②는 05에 없던 상태의 계약 공백 보완 ③은 "장애 시 오래된 결과라도"의 실행 경로 부재 발견 | ① 퍼지 매칭(식·요리 포함 여부): 오탐 위험, 힌트는 명시 어휘가 안전 ③ 즉시 RPC 추가: T6 차단 안 하는 백로그로 |
| 25 | 2026-08-11 (T0-2 실브라우저 검증) | NCP Maps(Web Dynamic Map) 실키가 `localhost:3010`에서 **인증 성공** — 실지도 렌더 확인. **08 잔여우려 1(카카오 폴백) 해소**: 폴백 착수하지 않는다 | 정찰 스펙으로 로그인→시드 여행→캔버스 진입 후 6초 대기(인증 실패는 mount 이후 비동기로 오므로) 판정: `naver.maps` 네임스페이스 존재·타일 이미지 56장·fake 폴백 배너 0건·인증 실패 배너 0건, 스크린샷 육안 확인(제주 지도+핀). 무료 이용량은 공식 정책상 Web Dynamic Map 월 600만 건이 **대표 계정**(Maps API를 처음 이용한 계정)에 제공 — 개인 사용량과 자릿수 차이가 커 과금 위험 낮음 | 카카오 폴백(RB-1) 착수: 실키 인증이 확인되어 불필요. 단 `provider` enum의 `kakao` 값(#16)과 `MapProvider` 추상화(#2)는 그대로 둔다 — 장래 폴백 여지를 없앨 이유가 없다 |
| 26 | 2026-08-11 (T5-3 구현) | 502 캐시 폴백을 `get_stale_search(qhash)`(새 RPC, 마이그레이션 `0006`)로 분리 — 5분 창을 무시하되 **상한 7일**. 정상 경로의 `get_cached_search` 5분 판정은 그대로 둔다 | 구현 중 발견: `upstreamProblem`이 `get_cached_search`를 다시 불렀는데, 같은 요청이 직전에 이미 그 함수로 miss를 낸 뒤라 **`cached[]` 분기가 도달 불가능한 죽은 코드**였다(기존 테스트는 실패하는 fetch 안에서 캐시를 채워 5분 이내로 만들어 이 사실을 가렸다). 두 조회는 묻는 것이 다르다 — 정상 경로는 신선도를, 폴백은 존재 여부를 묻는다. 상한을 둔 이유는 `search_cache`에 파기 주기가 없어 행이 영구히 남기 때문 — 무제한이면 몇 달 전 결과를 "검색 결과"로 내주게 된다. 문구도 "방금 받아둔"→"전에 받아둔"으로 정정(사실과 어긋남) | 같은 함수에 `max_age` 인자 추가: 호출부가 의도를 잃고 정상 경로에서 실수로 stale을 요청할 여지. 상한 없는 stale: 폐업·이전한 장소를 무기한 제공. 프록시에서 5분 판정을 걷어내기: 정상 경로의 중복 과금 방어선이 무너진다 |
| 27 | 2026-08-11 (사용자 피드백) | 새 여행 생성에서 **이름·날짜를 묻지 않는다** — "새 여행 만들기"가 곧 초안 생성(이름 '제목 없는 여행'·기간 오늘 하루)이고 바로 캔버스로 보낸다. 이름은 헤더의 `TripTitleField`, 기간은 그 옆 `DateRangeCalendar`(범위 선택 달력, 지난 날짜 비활성, 상한 없음)에서 고친다. 스키마는 그대로 둔다(start/end NOT NULL 유지) | "시작페이지도 날짜 설정하는거 굳이 이런 단계가 필요할까? 지도 페이지 우상단 같은곳에 저장할 수 있게" — 여행 계획은 "언제 갈지"보다 "어디 갈지"에서 시작하는 일이 많고, 기간 고치기 UI(FR-015)가 이미 헤더에 있어 나중에 고치는 경로가 이미 존재했다. 폼 단계가 사라지며 SC-001 계측도 1106ms→647~819ms 로 짧아졌다 | 스키마를 날짜 nullable 로: 마이그레이션 + days 지연 생성 + 일차 탭·타임라인 전반 수정이 필요한데, 기본값 하루로도 같은 사용자 가치를 얻는다. 이름만 받는 폼 유지: 물어볼 이유가 여전히 없다(빈 이름도 헤더에서 고칠 수 있다) |
| 28 | 2026-08-11 (울트라코드 리뷰에서 발견) | 달력 날짜 버튼에서 **mousedown 은 선택을 바꾸지 않는다** — "끌기 후보"만 기록하고 다른 날로 넘어갈 때(mouseenter)만 끌기로 승격한다. 끌기 뒤 따라오는 click 은 삼킨다 | 같은 버튼에 onClick·onMouseDown·onMouseUp 을 함께 걸었더니, 실제 포인터 순서(mousedown→mouseup→click)에서 두 번째 날의 mousedown 이 첫 앵커를 덮고 mouseup 이 앵커를 비워, **두 번 눌러 범위 잡기가 항상 하루짜리로 무너졌다**. 터치는 탭도 호환 마우스 이벤트를 발사하고 mouseenter 는 오지 않아 **모바일에서 여러 날 여행을 아예 만들 수 없었다**(주 타깃 390×844). 단위 테스트가 `fireEvent.click` 만 써서 322개 통과가 이 결함을 가렸다 — 실제 이벤트 순서(mousedown→mouseup→click) 회귀 테스트 2건과 E2E 기간 반영 단언을 함께 추가했다 | Pointer Events + 이동 임계값: 지금 필요보다 복잡. click 만 남기고 끌기 제거: 데스크톱 편의를 잃는다 |
| 29 | 2026-08-11 (사용자 피드백) | 지도에서 직접 찍는 경로에 **보이는 문**을 만든다 — 리스트 위 "지도에서 찍기" 버튼이 모드를 켜고, 켜진 동안의 지도 탭 한 번이 미니 폼을 연다(`MapProvider.onMapTap` 신설). 롱프레스·우클릭은 그대로 남긴다 | "검색을 통해서만 추가하는게 아니라 실제 위치 클릭해서 지정할수 있으면" — 기능(FR-016)은 T6-5에 이미 있었으나 진입점이 롱프레스·우클릭뿐이라 발견 불가였고, 안내 문구도 검색 0건일 때만 떴다. 그냥 좌클릭에 바로 걸지 않은 이유: 지도 이동·핀 선택과 부딪힌다 — 모드를 켠 화면만 탭을 소비한다. 버튼에 강조색을 쓰지 않은 이유: 이 화면의 주 행동은 여전히 검색 입력이다 (L-09) | 좌클릭 상시 수신: 지도를 못 쓰게 된다. 안내 문구만 상시 노출: 읽어도 롱프레스를 해야 해 터치 발견성이 그대로다 |
| 30 | 2026-08-11 (사용자가 실제로 막힘) | 거부된 세션은 **쿠키를 지우고** `/login?reason=session-ended` 로 보낸다(`updateSession`). 단 서버에 닿지 못한 실패(`AuthRetryableFetchError`)는 절대 지우지 않고, `/auth/confirm` 경로도 제외한다 | 사용자 브라우저 토큰의 `iat` 가 미래로 찍혀(Supabase 컨테이너 시계가 앞섰던 시점 발급) 요청마다 거부됐고, 미들웨어가 쿠키를 남긴 채 307 만 돌려줘 **같은 실패가 무한 반복**됐다. 화면에는 "눌러도 반응 없음"으로 보였다. 예외 두 개의 이유: ① 일시적 불통으로 멀쩡한 사용자를 로그아웃시키면 안 된다 ② `/auth/confirm` 은 낡은 쿠키를 든 채 도착해 **새 세션을 심는** 자리라 여기서 지우면 로그인이 영영 안 끝난다 | 무조건 삭제: Supabase 가 잠깐 멈출 때마다 전원 로그아웃. 아무것도 안 함: 개발자도구 없이는 못 빠져나온다 |
| 31 | 2026-08-11 | 로그아웃 경로를 `/auth/signout`(POST, Route Handler)로 만든다 — `signOut({ scope: 'local' })` + **성공 여부와 무관하게** 쿠키 만료 + 303. 버튼은 `'use client'` 없는 `<form method="post">` | 세션이 꼬였을 때 빠져나올 문이 앱에 없었다(로그아웃 UI 0건). `/auth/` 는 이미 세션 없이 열리는 표면이라 규칙을 더할 필요가 없다. 폼 POST 인 이유: JS 없이도·세션이 깨져도 눌린다. GET 링크로 두면 prefetch 가 로그아웃을 실행한다. **scope 를 명시하는 이유**: auth-js 기본값이 `'global'` 이라 생략하면 그 계정의 모든 기기가 로그아웃된다 — 결정 #13 대로 한 사람이 PWA·Safari 두 세션을 갖는 게 정상인 앱에서, 하필 에러 화면에 놓인 버튼이 그런 폭발 반경을 갖는다 | 확인 묻기: 되돌릴 수 있는 일이라 안 묻는다(T-06 과 같은 기준). GET: prefetch 사고 |
| 32 | 2026-08-11 (울트라코드 검증에서 발견) | 로그아웃 리다이렉트의 `Location` 을 **Host 헤더로 직접 짓는다**(`loginRedirectTarget`). `/login` 사연은 `Map` 으로 조회한다 | ① Route Handler 의 `request.nextUrl.origin` 은 클라이언트가 접속한 호스트가 아니라 **Next 가 바인드한 주소**다(`attachRequestMeta` — `fetchHostname`·`port` 가 있으면 Host 를 안 본다). 폰에서 LAN IP 로 들어온 사용자가 `localhost` 로 튕겨 탈출구가 연결 실패로 끝난다. 상대 경로도 답이 아니다 — Next 가 같은 주소로 다시 절대화한다(실측). 목적지 경로를 우리가 고정하므로 Host 를 믿어도 열린 리다이렉트가 아니다. ② 평범한 객체로 사연을 조회하면 `?reason=toString` 이 **함수**를 돌려주고 React 가 렌더하려다 500 으로 죽었다(실측: `toString`·`__proto__`·`constructor`·`hasOwnProperty` 전부 500) | ① `trustHostHeader` 설정: 이 한 곳을 위해 서버 전역 신뢰 모델을 바꿀 이유가 없다 ② `Object.hasOwn` 가드: `Map` 이 의도를 그대로 드러낸다 |
| 33 | 2026-08-12 (사용자 요청) | 카카오 로그인을 **주 경로로 추가**하되 메일 OTP 는 그대로 남긴다. OAuth 착지점은 `/auth/callback`(신설, `exchangeCodeForSession`)이고 메일 링크(`/auth/confirm`, `verifyOtp`)와 섞지 않는다 | 소셜 로그인은 02-blindspot-register 에서 "Assumed: non-goal" 로 **확인 없이 범위 밖 처리**됐던 항목이다(사용자 지적으로 발견). 카카오는 결정 #13 이 풀려던 문제(iOS 설치형 PWA 에서 매직링크를 누르면 세션이 Safari 에 생긴다)를 더 잘 푼다 — OAuth 는 앱 안에서 시작해 앱으로 돌아오므로 그 함정 자체가 없다. 메일 OTP 를 남기는 이유: E2E 5개가 Mailpit 으로 자동화돼 있는데 카카오 동의 화면은 카카오 도메인이라 자동화가 닿지 않는다. 카카오 버튼에 `bg-foreground` 를 쓰지 않아 강조 CTA 는 '인증 코드 받기' 하나로 유지된다 (L-09) | 카카오로 완전 전환: E2E 인증 경로 전부 재설계 + 카카오를 못 쓰는 상황(동의 취소·팀원 미등록·이메일 미제공)의 대안 소멸 |
| 34 | 2026-08-12 | 인증 왕복의 되돌아갈 주소를 `appRedirectTarget(request, path)` 한 곳에서 짓는다 — Host 헤더 기준. `/auth/confirm` 의 기존 `nextUrl.origin` 사용도 함께 고쳤다 | 결정 #32 에서 로그아웃만 고쳤는데 같은 함정이 인증 왕복 전체에 있었다: Route Handler 의 `nextUrl.origin` 은 Next 가 바인드한 주소라 폰에서 LAN IP 로 들어온 사용자가 메일 링크를 열면 localhost 로 튕긴다. 라우트마다 같은 방식으로 틀리느니 규칙을 한 곳에 둔다 | 라우트별 처리: 새 라우트를 만들 때마다 같은 실수가 반복된다 |
| 35 | 2026-08-12 (사용자 요청) | 무료 티어가 조용히 꺼지는 것을 `keepalive` 워크플로로 막는다 — 매일 Supabase REST 를 한 번 찌르고(1주 무활동 일시정지 방지), 매월 1일 빈 커밋으로 저장소 활동을 남긴다(60일 무활동 시 스케줄 워크플로 비활성화 방지) | Supabase 무료는 1주, GitHub 는 60일이 기준이고 **워크플로 실행 자체는 저장소 활동이 아니다**. 60일 규칙은 공식 문서가 public 저장소라고 적고 있으나 private 에서도 겪었다는 보고가 섞여 있어 어느 쪽이든 안전하게 둔다. secret 이 없으면 실패 대신 안내만 남기고 통과시킨다 — T0-3 전에는 저장소만 있어도 워크플로가 붉게 물들면 안 된다 | 별도 모니터링 서비스: 개인 도구에 과잉. 앱에서 self-ping: 앱이 안 떠 있으면 무의미 |
| 37 | 2026-08-12 (실측으로 확정) | 카카오 인가 요청에는 **nonce 의 SHA-256 해시**를 싣고, `signInWithIdToken` 에는 **원문**을 넘긴다 | 평문을 양쪽에 쓰면 GoTrue 가 `400 Nonces mismatch` 로 거절한다. 진단 로그로 대조해 보니 카카오가 돌려준 토큰의 nonce 는 우리가 보낸 값과 **정확히 같았다** — 즉 GoTrue 는 평문끼리 비교하지 않고 **받은 nonce 를 해시해 토큰 값과 대조**한다(구글 안드로이드 방식). 해시를 보내도록 바꾸자 곧바로 통과했고, 실브라우저로 카카오 로그인→동의→여행 목록 도달까지 확인했다 | 에러 이름만 로깅: "AuthApiError" 만으로는 사유를 알 수 없어 한 번 더 시켜 봐야 했다 — 그래서 status·message 까지 남기도록 바꿨다. `skip_nonce_check = true`: 이유를 모른 채 보안 검사를 끄는 선택이었을 것이다(원인이 우리 쪽 구현이었다) |
| 38 | 2026-08-12 (원격 반영 중 발견) | 테이블 권한을 **명시적으로 회수**한다 (`0007_revoke_anon.sql`) — 지금까지는 로컬 기본값에 기대고 있었다. 검증도 RLS 경유가 아니라 `has_table_privilege` 로 직접 묻는다(`supabase/tests/grants.sql`) | 원격 프로젝트에 마이그레이션을 반영한 뒤 대조하니 `anon` 의 select 권한이 **로컬 false / 원격 true** 였다. 클라우드 프로젝트는 생성 시 public 스키마에 anon·authenticated 기본 권한을 걸어 두는데 `0002_rls.sql` 에는 revoke 가 한 줄도 없었다. 유출은 없었다(RLS 가 켜져 있고 운영 테이블은 정책이 없어 전면 deny) — 그러나 ① 05 §권한 모델·결정 #11 의 의도와 어긋나고 ② pgTAP 의 "permission denied" 단언이 원격에서는 성립하지 않아 **테스트가 운영을 설명하지 못했고** ③ 정책을 하나 잘못 열면 그 순간 grant 가 살아난다. `alter default privileges` 로 앞으로 추가될 테이블도 같은 규칙을 따르게 했다 | "RLS 가 막아 주니 그대로 두기": 다층 방어를 한 겹 버리는 선택이고, 무엇보다 테스트가 거짓이 된다 |
| 39 | 2026-08-18 (사용자 요청) | 보관함 단계에도 **예상 금액**을 남긴다 — `places.estimated_cost`(원 단위 정수, 선택 입력, `0008`). 실제 지출(`stops.cost_amount`)은 그대로 두고 **두 컬럼이 공존**한다. 화면 합계도 **확정 / 예상 포함 두 줄**로 나누고, 자동 복사(예상→실제)는 넣지 않는다. 보관함(미배치)은 총액에 안 섞고 보관함 안에서만 소계를 낸다 | 결정 #24 를 뒤집는 것이 아니다. #24 가 `places.cost_amount` 를 거부한 근거 두 가지 중 "같은 장소 2회 방문의 지출은 각각"은 **실제 지출**에만 해당하고 여전히 옳다 — 그래서 실제는 Stop 에 둔다. "보관함 가격은 확정 전 추정치"는 "적지 말자"가 아니라 "추정치로서 적자"가 사용자 요구였다("식당 예상 금액을 저장해 경비 계산에 쓰고 싶다"). 추정 단가는 방문이 아니라 **장소의 성질**이다. 두 숫자를 하나로 합치지 않는 이유: 합치는 순간 화면에서 "이미 쓴 돈"을 되찾을 수 없고, 그 구분이 이 기능의 존재 이유다. 0원과 미입력(null)은 다른 값으로 유지 | 한 줄 합계(실제 있으면 실제, 없으면 예상): 확정분과 추정분이 섞여 구분 불가. 예상→실제 자동 채움: 한 번 복사되면 "적어 둔 값"과 "정말 쓴 값"이 같아져 기능이 무의미해진다. 보관함까지 총액 합산: 갈지 안 갈지 모르는 후보로 총액이 부풀어 보인다 |
| 40 | 2026-08-18 (사용자 지적 → 실측) | 클릭으로 연 장소 카드는 리스트 패널이 아니라 **지도 위에서 그 핀에 붙어** 뜨고, 지도 이동·확대축소를 따라간다. `MapProvider` 에 `project(latLng)`·`onViewportChange(cb)` 를 추가해 UI 는 여전히 SDK 를 직접 만지지 않는다 (#2 유지) | "카드형으로 떠서 수정"을 패널 안 렌더로 구현했더니 사용자가 "왼쪽 패널에 여전히 있는데"라고 지적했다. 이어 "확대축소를 하든 따라오게 해야"까지 나왔다 — 화면 모서리 고정은 지도 위 카드가 아니다. **실측으로 잡은 함정**: `getProjection().fromCoordToOffset()` 은 화면 좌표가 아니라 **현재 줌의 월드 픽셀**이라 지도를 끌어도 값이 고정이었다(1058.999/441.96). 뷰포트 북서 모서리(NE.lat + SW.lng)를 투영해 빼야 컨테이너 기준이 된다. 구독은 `idle` 만으로 부족하다 — 끄는 동안 카드가 뒤처진다 | 화면 모서리 고정: 지도를 옮기면 카드가 가리키는 곳을 잃는다. SDK InfoWindow: React 폼(메모·금액 입력)을 담을 수 없고 #2 의 추상화도 깨진다. 렌더 중 `offsetHeight` 측정으로 뒤집기 판정: `react-hooks/refs` 위반이고 측정 후 재렌더로 카드가 한 번 튄다 — 상수 임계값으로 대신했다 |
| 41 | 2026-08-18 (사용자 요청) | 핀은 **두 채널**로 말한다 — **색 = 몇 일차인가**, **모양 = 무엇을 하는 곳인가**. 일차에 배치된 곳은 일차 색 원에 **일차 번호**를, 보관함은 카테고리 색 원에 **카테고리 아이콘**을 낸다. 일차 색은 `days.color`(토큰, `0009`)에 저장하고 **고정 팔레트 8색**에서 고른다. 안 고르면 `position` 순서로 팔레트를 돈다 | 카테고리 3색과 일차 색이 **같은 채널(색)** 을 쓰면 둘 다 못 읽는다. 사용자 제안("카테고리는 아이콘으로 하고 일자는 숫자 아이콘으로")이 채널을 갈랐다. 20px 원 안에 숫자와 아이콘을 **동시에** 넣으면 둘 다 안 읽혀서 배치 여부로 하나만 낸다 — 배치된 곳의 카테고리는 리스트·카드에서 계속 보인다. hex 가 아니라 토큰을 저장하는 이유: 실제 색값은 테마가 정할 몫이고 DB 가 떠안을 이유가 없다. 팔레트를 CHECK 로 가두는 이유: 자유 색은 흰 숫자가 안 보이는 조합(노랑 등)을 허용한다 | 자유 색(컬러 피커): 대비가 깨져 핀 안 숫자가 사라진다. 카테고리 색까지 사용자 지정: 저장할 곳이 한 군데 더 필요하고, 색이 일차 채널로 넘어간 마당에 카테고리 색을 고르게 할 이유가 약하다. 숫자 + 작은 카테고리 점 동시 표시: 390px 에서 점이 4px 남짓이라 색 구분이 안 된다. `days.color` 에 DB 기본값 박기: 모든 일차가 같은 색으로 시작해 만들자마자 구분이 안 된다 |
| 42 | 2026-08-19 (사용자 피드백) | 모바일은 **네이버 지도식 하단 메뉴**(지도·보관함·일정)로 화면을 가른다. 지도가 기본 화면이고 패널은 갈아탈 때만 나온다. **검색은 화면 맨 위 상주 바**로 빼되 인스턴스는 하나다. 데스크톱 사이드바 구조는 그대로 | "모바일에서 보니까 지도가 너무 작아" — 하단 시트가 46% 를 상시로 먹고 있었다. 처음엔 '설정 아이콘으로 열기'로 갔다가 사용자가 "하단에 메뉴를 만들어 분리"로 방향을 잡아 줬다. 담아둔 곳이 없을 때만 보관함으로 시작한다: 지도에 볼 것도 없는데 담는 문까지 숨기면 새 여행이 막다른 화면이 된다. **실측으로 잡은 것 둘** ① `h-full` 은 flex 아이템 안에서 퍼센트 기준이 확정되지 않아 지도 컨테이너가 0px 이 되고 지도가 백지로 떴다 → `absolute inset-0` 로 채운다 ② 검색을 모바일·데스크톱 두 벌 두면 `id="place-search"` 가 문서에 두 번 생겨 라벨이 엉뚱한 입력에 붙는다 → 하나만 둔다 | 시트 높이만 키우기: 지도는 여전히 절반이다. 설정 아이콘 뒤에 숨기기: 자주 쓰는 두 화면(보관함·일정)이 한 겹 더 들어간다. 검색을 패널 안에 두기: 패널을 닫으면 담는 문이 사라진다 |
| 43 | 2026-08-19 (사용자 요청) | 장소 카드가 **일정에 넣고 빼는 자리**가 된다 — 이름·카테고리·주소 위에 `일정에 넣기`(일차 고르기)/`일정에서 빼기`, 그 아래 메모·예상 금액·사진. 카드는 핀 위 공간에 맞춰 위/아래를 고르고 **쓸 수 있는 높이를 max-height 로 받아** 그 안에서 접힌다 | "일정에 추가하고 말고를 해당 스팟을 클릭하면 네이버 장소처럼 카드를 구성해 주면 좋겠어 그 내용을 내가 채울 수 있도록" — 장소를 들여다보는 자리와 정하는 자리가 갈려 있으면 리스트로 되돌아가야 한다. 높이를 함께 정하는 이유: 카드가 핀 위 공간보다 크면 화면 위로 밀려 나가 제목과 넣기 버튼이 잘렸다(실측). 렌더 중 `offsetHeight` 를 못 읽으므로(react-hooks/refs) 높이를 재는 대신 **공간을 넘겨준다** | 카드에서 리스트로 보내기: 두 번 이동해야 한다. 넣기 버튼에 강조색: 이 카드의 주 행동은 내용을 채우는 것이다 (L-09). 측정 후 재배치: 카드가 한 번 튄다 |
| 44 | 2026-08-19 (사용자 요청) | 하단 패널은 **손잡이를 아래로 쓸어** 내린다(60px 임계). 검색 입력에는 **지우기 X** 를 직접 둔다. 끌기는 Pointer Events + `setPointerCapture` 로 받고, 끌기 뒤 따라오는 click 은 삼킨다 | "보관함, 일정도 스와이프로 내렸을 때 내려가고 지도가 보이게", "검색할 때 x 아이콘 만들어서 텍스트 초기화". **실측으로 잡은 것 둘** ① 캡처가 없으면 손잡이 높이(약 44px)를 벗어나는 순간 `pointermove` 가 끊겨 임계값에 영영 닿지 못한다 — 브라우저에서 끌어 보고서야 나왔다(유닛 테스트는 좌표를 직접 쏘므로 통과했다) ② `type="search"` 의 네이티브 X 는 브라우저마다 있거나 없어 모바일에서 기댈 수 없다. click 삼키기는 결정 #28 과 같은 계열이다 — 안 삼키면 내리자마자 토글이 다시 연다 | 터치 이벤트만 쓰기: 데스크톱 마우스 끌기를 못 받는다. 임계값 없이 방향만 보기: 손 떨림이 패널을 닫는다. 위로 끄는 것도 받기: 더 올릴 자리가 없어 뜻이 없다. 네이티브 X 에 기대기: 안드로이드 크롬에 없다 |
| 45 | 2026-08-20 (T10-5 구현·전환) | 일차의 이동시간은 **NCP Directions 15**(`maps.apigw.ntruss.com/map-direction-15/v1/driving`, `option=traoptimal`)를 서버 프록시(`/api/directions`, E-15)로 부른다 — 방문들을 목록 순서대로 이어 **한 일차에 한 번**. 자동차만 낸다. 구간 시간은 `summary.waypoints[]` + `summary.goal` 을 이어 만들고 **밀리초를 초로** 바꾼다. 사이에 사용자가 적어 둔 이동(Leg)이 있는 구간은 추정치를 내지 않는다. 화면은 `useDayRoute`(React Query 미사용)가 좌표 목록을 키 삼아 받아 두고, 실패(401·502·취소)는 조용히 넘어간다 | 지도·검색이 이미 네이버라 **제공자·콘솔·쿼터가 한 곳으로 모인다**(카카오 키는 로그인 #33 전용으로 남는다). 경유지 길찾기라 구간마다 따로 부를 때의 n-1 호출이 한 번으로 준다. **유류비(`fuelPrice`)까지 온다** — 경비 요약(#39)에 그대로 값이 된다(카카오엔 없다). 시크릿은 헤더로만 싣는다 — 주소창에 실으면 로그에 남는다. 로그인 안 한 요청은 업스트림 전에 막는다(남의 요청으로 쿼터를 태울 이유가 없다). Leg 구간을 비우는 이유: 예매한 기차가 있는데 자동차 추정치를 끼워 넣으면 화면이 두 말을 한다 — **기록이 추정보다 세다**(#39 의 예상 vs 실제와 같은 원칙). 순서가 곧 이동 순서라 좌표 목록이 그대로 캐시 키가 된다(#15). React Query 를 안 쓴 이유: 이 훅은 `TimelinePane` 안쪽 깊은 곳에서 쓰이는데 Provider 를 요구하면 그 컴포넌트를 단독 렌더하던 테스트가 전부 Provider 를 끌고 와야 한다. 실패를 안 띄우는 이유: 이동시간은 곁들이는 정보이지 여정의 전제가 아니다 | **카카오모빌리티 길찾기(처음 구현했다가 되돌림)**: 인계 문서의 "NCP 무료 이용량에 Directions 없음"을 근거로 골랐는데 **그 전제가 틀렸다**(2026-08-20 실측: 콘솔에 Directions 5 월 60,000회·Directions 15 월 3,000회 무료, 기존 NCP 키로 HTTP 200). 같은 좌표(제주 공항→중문→성산)로 두 곳을 실제로 불러 비교했더니 53분+90분 vs 53분+93분으로 차이가 없었고, NCP 만 유류비를 준다. 카카오를 남길 이유는 이미 쓰인 코드뿐이라 되돌렸다. **Directions 5**: 경유지 6개를 넣으면 `400 too many waypoints (max 5)` — 일차당 7곳은 모자란다. 15 하나만 쓰면 17곳까지 분기 없이 덮고 월 3,000회면 하루 100일차라 개인용에 남는다. 구간별 개별 호출: 방문 8곳이면 7번 — 쿼터·지연 모두 손해. 클라이언트에서 직접 호출: 시크릿이 번들에 노출된다. 대중교통: 공개 API 가 없어 ODsay 등 별도 가입이 필요 — 사용자가 원하면 그때. 도보: 아직 안 넣었다(외부 API 없이 좌표 거리로 추정 가능). 실패를 에러 화면으로: 길찾기 쿼터가 떨어졌다고 일정 화면이 붉어질 이유가 없다 |
| 46 | 2026-08-20 (사용자 요청) | 어디를 갈지 **별표로 협의**한다 — `place_votes`(place_id + voter_key 복합 PK, stars 1~3, `0010`). **계정을 요구하지 않는다**: 공유 링크를 가진 사람이면 `vote_shared_place(token, place_id, voter_key, stars)` SECURITY DEFINER RPC 로 누른다. anon 은 테이블 권한이 없다. stars=0 은 취소다. 표는 **장소**에 달린다(일차 배치가 아니라) | 여행은 혼자 정하지 않는데 이 앱의 공유는 읽기 전용 링크뿐이라(#3) 동행자가 "나 여기 가고 싶다"를 말할 자리가 없었다. **정식 초대·다계정으로 안 간 이유**: #3 을 뒤집으면 trip_members·초대 수락 플로우·전 테이블 RLS 재작성·pgTAP 재검토가 따라오는데, 여행 한 번 때문에 동행자에게 가입을 시키면 애초에 아무도 안 누른다. voter_key 는 브라우저 난수다 — 신원이 아니라 중복 방지용이라 개인정보가 아니고, 지우면 새 사람이 된다(링크를 아는 사람끼리 쓰는 도구엔 그 정도면 된다). stars 를 1~3 으로 둔 이유: 5단계는 "3과 4의 차이"를 아무도 설명하지 못한다. 표를 장소에 다는 이유: 아직 어디에 넣을지 모르는 **보관함 후보**를 두고 이야기하는 것이 이 기능의 목적이다 | 정식 초대·계정: 참여 모델 전면 재설계 — 규모가 이 기능의 값어치를 넘는다. 표를 stops 에 달기: 일차에 넣기 전에는 이야기할 수 없게 된다. anon 에게 테이블 직접 쓰기 허용: 링크 하나로 남의 여행에 표를 넣을 수 있다 — 토큰·소속 검증을 클라이언트에 맡기는 셈이다. 익명 무제한 투표(voter_key 없음): 한 사람이 눌러 표를 부풀린다 |
| 47 | 2026-08-20 (사용자 요청) | 일차 안의 방문에 **확정/미확정**을 둔다(`stops.confirmed`, `0011`). 경로는 **확정된 것만** 목록 순서대로 잇는다 — 미확정은 아예 없는 셈 치고 앞뒤를 잇되, 사이의 이동(Leg)이 구간을 끊는 규칙(#45)은 그대로 산다. **기본값은 true** | 별표(#46)로 협의가 끝난 곳과 아직 이야기 중인 곳이 한 일차에 섞이는데, 둘을 다 이어 경로를 내면 갈지 모르는 곳까지 낀 시간이 나와 하루가 실제보다 길어 보인다. **기본값이 true 인 이유**: 일차에 넣는 행위 자체가 이미 "가기로 했다"는 뜻이다. false 로 두면 ① 이미 있는 여행들의 경로가 전부 사라지고 ② 장소를 담을 때마다 확정 체크를 한 번 더 눌러야 해 한 번이면 될 일이 두 번이 된다 — 예외를 표시하는 쪽이 짧다. **표가 임계값을 넘으면 자동 확정**으로 안 한 이유: 동행자가 별을 누를 때마다 경로가 저 혼자 바뀌고, 화면이 왜 바뀌었는지 아무도 설명하지 못한다. 별표는 의견을 모으고 확정은 주인이 정한다 | 자동 확정(별표 임계값): 기준값의 근거가 없고 화면이 예측 불가능해진다. 새 상태 없이 두기: "확정된 일정만 경로"라는 요구 자체가 성립하지 않는다. 기본값 false: 기존 여행의 경로가 통째로 사라진다(회귀) |
| 48 | 2026-08-20 (사용자 요청) | 시각 언어를 **Toss Design System 각색본**으로 바꾼다 — 토큰은 `src/app/globals.css`, 규약은 `docs/design/design-system.md`. 서체는 Pretendard(CDN), 본문 15/1.5, 라운드 9단·모션 3토큰·1px 헤어라인·평면 우선을 그대로 채용한다. **가장 큰 각색: 색의 역할을 둘로 가른다** — 액션 색은 브랜드 블루 하나(CTA·포커스·활성 탭), 데이터 색은 일차 8색(#41)·카테고리 3색(핀·배지 전용, 버튼 금지). 다크 alias 는 이 앱이 정의한다 | TDS 의 핵심 규율은 "한 화면에 강조색 하나"인데 이 앱은 색이 이미 정보 채널이다 — 규율을 그대로 들이면 지도가 말을 잃고, 버리면 화면이 알록달록해진다. TDS 자신도 이 구분을 한다(bar-chart 막대는 grey-200, 강조 막대만 브랜드색). 이 앱 `globals.css` 에도 원래 "정보 전달용이라 화면당 강조 1곳과 충돌하지 않는다"는 같은 논리가 적혀 있었다. **다크를 우리가 정하는 이유**: TDS 핸드오프 번들은 라이트 값만 공개돼 있다(원본 Known Gaps). 회색 사다리를 뒤집고 블루는 어두운 바닥에서 읽히도록 한 단계 밝히되 hue 는 고정하며, pressed overlay 는 검정 26% 로 안 보여 흰빛 14% 로 뒤집었다. **Pretendard 를 npm 대신 CDN 으로** 받는 이유: 이 저장소의 lockfile 은 리눅스에서 만들어야 CI 가 통과한다 | TPS 원본 서체: 외부 배포가 라이선스로 막혀 있다. 일차 색을 버리고 브랜드색 하나로: 지도에서 일차를 구분할 방법이 사라진다(#41 을 뒤집는 것). 일차 색에 다크 변형 추가: 이 색이 놓이는 바닥은 테마를 안 따르는 지도 타일이라 같은 일차가 두 색으로 기억된다. 토스의 bottom-CTA 규율 채용: 이 앱의 모바일 하단은 네비게이션 탭(#42)이 이미 차지한다 — 겹쳐 쌓으면 위계가 무너진다. 토스 금융 도메인(송금 플로우·keypad) 이식: 원본 Don'ts 가 명시적으로 금지한다 |
| 49 | 2026-08-20 (사용자 지적) | 지도에서 **순서와 경로를 읽을 수 있게** 한다 — ① 핀 숫자를 '몇 일차'에서 **'그 날 몇 번째 방문'**으로 바꾼다(`Pin.orderNumber`) ② 그 날 실제로 달리는 길을 **일차 색 선**으로 그린다(`MapProvider.setRoutePath`, NCP 응답의 `path` 좌표). 색은 그대로 '몇 일차'를 나른다(#41) | **숫자가 낭비되고 있었다**: 색도 일차, 숫자도 일차라 같은 말을 두 번 하고 있었고, 그동안 정작 순서를 읽을 방법이 없어 한 일차의 핀이 전부 같은 숫자였다("다 같은 숫자로 연결되어 있어서 순서 파악이 힘들다"). 숫자를 순서로 돌리면 채널이 갈린다 — **색 = 어느 날 · 숫자 = 몇 번째**. 선을 함께 그리는 이유: 숫자만으로는 눈이 1→2→3을 스스로 이어야 하고, 핀이 흩어져 있으면 그 일이 어렵다. **직선으로 잇지 않는 이유**: 제주에서 두 점을 직선으로 이으면 바다를 가로지른다 — 실제 도로 좌표(`route.traoptimal[0].path`)를 쓴다. 선을 일차 색으로 칠하는 이유: 선과 핀이 같은 일차로 읽혀야 여러 날이 겹쳐 보일 때 구분된다. 경로를 타임라인이 받아 지도로 **알리기만** 하는 이유: 시간 표시가 타임라인의 일이고 그리기는 지도의 일이라, 상태를 두 곳이 각자 들면 어긋난다 | 숫자를 그대로 두고 작은 순서 배지를 덧붙이기: 20px 원 안에 숫자 둘은 어차피 안 읽힌다(#41 에서 이미 겪었다). 선을 중립 회색으로: 일차가 둘 이상 보일 때 어느 선이 어느 날인지 사라진다. 직선(대권) 연결: 제주·섬 지역에서 바다를 가로지르는 거짓 경로가 된다. 경로 상태를 CanvasBoard 가 직접 가져오기: 어느 일차가 열려 있는지는 ListPane 이 쥐고 있어 그 상태를 위로 끌어올려야 하고, 타임라인이 이미 같은 답을 받고 있는데 호출을 두 번 하게 된다 |
| 50 | 2026-08-20 (사용자 지적) | 모바일에서 **갇히지 않게** 세 곳을 고친다 — ① 하단 메뉴는 **지도를 만지는 동안만** 물러난다(카드가 열린 것으로는 숨기지 않는다) ② 지도 컨테이너가 하단 메뉴 위에서 끝난다(`--mobile-nav-h`) ③ 장소를 담으면 **모바일에서만** 그 카드를 지도 위에 연다 | ① 결정 #48 에서 "지도를 넓게" 쓰려고 카드가 열리면 탭을 숨겼는데, **조작은 순간이지만 카드는 머무는 상태**라 그동안 메뉴를 누를 수 없었다("카드가 뜬 상태에서는 메뉴가 눌리지가 않는다"). 숨기는 값은 순간에만 치른다. ② SDK 가 컨테이너 맨 아래에 붙이는 네이버 로고·저작권이 탭에 가렸다. 탭이 불투명해 가려진 지도는 어차피 안 보이므로 컨테이너를 줄이는 편이 낫고, 그러면 컨테이너가 보이는 영역과 같아져 핀에 붙는 카드의 좌표 계산도 정확해진다. 높이를 한 변수에서만 정하는 이유: 탭과 지도가 각자 숫자를 들면 반드시 어긋난다. ③ 담은 곳은 이미 지도에 핀으로 있었지만(미배치는 카테고리 색+아이콘) **저장 뒤 화면이 아무 말을 안 해** 어디 갔는지 몰랐다. 카드를 여는 것이 곧 "여기 담겼어요"라는 답이고, 그 카드에서 바로 일차를 정한다(#43). **데스크톱을 제외한 이유**: 거기서는 목록과 지도가 함께 보여 담긴 것이 바로 눈에 들고, 카드를 열면 패널이 상세로 갈아타 **여러 곳을 연달아 담지 못한다** — E2E(여정 1)가 이걸 잡았다 | ① 카드가 열려도 계속 숨기기: 메뉴가 사라져 화면에 갇힌다. ② CSS 로 로고만 끌어올리기: SDK 가 주는 안정적인 클래스가 `.map_copyright` 하나뿐이라 나머지는 인라인 스타일 div 라 선택자가 부서진다. 컨트롤 위치 옵션은 모서리만 고를 수 있고 오프셋이 없다. ③ 저장 후 지도만 움직이기(panTo): 어디로 갔는지는 알지만 일차를 정하려면 다시 찾아 눌러야 한다. 데스크톱에서도 카드 열기: 연달아 담는 흐름이 끊긴다 |
