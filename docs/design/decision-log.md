# Decision Log — 여행 플래너 "Trip Canvas"

| # | 일자 | 결정 | 선택 이유 | 버린 대안과 이유 |
|---|---|---|---|---|
| 1 | 2026-08-06 | 플랫폼 = 반응형 웹 + PWA (Next.js, Vercel 배포) | 아이폰(여행 중)+데스크톱(계획) 한 코드베이스, iOS 26 홈화면 웹앱 기본 실행, 비용·심사 0. 필요 기능 전부 PWA 범위 내 (01-recon 플랫폼 조사) | 네이티브 iOS: 연 $99+심사+Swift 학습, 계획용 데스크톱 화면 부재. Capacitor 래핑 전제: 현 시점 이득 없음 — 필요해지면 그때 도입해도 손해 없음 |
| 2 | 2026-08-06 | 지도 = 국내 네이버(MVP) → 해외 구글(2단계), MapProvider 추상화 계층 필수 | 사용자 지정(습관·국내 POI·네이버 상세 링크 연결 가치). 지역 검색 API 일 25k 무료 확인 | 구글 단일(추천안): 사용자가 국내 POI 품질·네이버 습관을 우선함. 카카오: 네이버 무료 이용량 조건 미충족 시 폴백으로 유지 (탈락 아닌 대기) |
| 3 | 2026-08-06 | 사용 주체 = 1계정(매직링크) + 여행별 읽기전용 공유 링크 토큰 | 검증된 최소 공유 모델(트리플·Wanderlog), 인증·격리 비용 최소 | 완전 개인용: 동행자 공유 가치 포기 대비 절감 미미. 공개 서비스: 개인정보·인증·비용 과투자 |
| 4 | 2026-08-06 | 장소 등록 = 앱 내 검색(네이버 지역 검색 API 서버 프록시) | 클릭 최소·안정적. 네이버지도 저장목록 공식 연동 부재가 확정 사실 (01-recon 핵심 발견 1) | 공유링크 파싱: 비공식 포맷 의존 취약 → 2단계 후보로 기록. 저장목록 자동 연동: 기술적 불가 |
| 5 | 2026-08-06 | 장소 사진 = 사용자 업로드(클라 리사이즈) + 네이버 상세 링크 보완. 구글 단계에서 해외만 자동 사진 | 국내 검색 API 사진 미제공 확정 (카카오도 동일 — 01-recon 핵심 발견 2). 스크래핑은 약관 리스크로 금지 | 네이버 이미지 검색 API 자동 매칭: 정확도·약관 불확실 → 2단계 검토 후보 |
| 6 | 2026-08-06 | 좌표는 WGS84(lat/lng)로 정규화 저장, KATECH(mapx/mapy)은 입수 시 즉시 변환 | 제공자 교체(구글·카카오)에도 데이터 불변 — MapProvider 추상화의 전제 | 제공자 원시 좌표 저장: 제공자 락인, 이중 표시 버그 원천 |
| 7 | 2026-08-06 | 이동(Leg) = 수동 입력 폼 + 일자 타임라인 표시. 예매처 연동 non-goal | 국내 예매처(코레일·고속버스 등) 공개 API 부재. 수동 입력도 "한눈에 보기" 가치는 충족 | 메일 파싱·예매처 연동: 유지보수 대비 가치 낮음(개인 도구) |
| 8 | 2026-08-06 | 미배정 장소 "보관함" 분리 — 수집(저장)과 배치(일정)를 다른 상태로 모델링 | 도메인 업무 흐름의 1단계(수집)와 2단계(배치)가 실제로 분리돼 있음 (01-recon) | 저장 즉시 날짜 강제 배정: 수집 마찰 증가 — "일단 저장"이 핵심 사용 패턴 |
| 9 | 2026-08-06 | 스택 = Next.js + Tailwind + Supabase(Auth·Postgres·Storage) + Vercel | 1인 개발·무료 티어·지도 JS SDK 호환 fit (01-recon 스택 표) | Firebase: Postgres(관계 모델 — Trip/Day/Stop)가 문서형보다 적합. 로컬 IndexedDB 단독: 공유 링크 불가 |
| 10 | 2026-08-06 | 공동 편집(실시간 협업) 명시적 non-goal | 읽기전용 공유로 동행자 가치 80% 충족, 실시간 충돌 처리는 1인 개발 범위 초과 | Wanderlog식 공동 편집: 차별 요소지만 비용 과다 — 범위 방어 우선 |
| 11 | 2026-08-06 (GATE 1차) | 운영 테이블(search_cache·api_usage) 쓰기 = SECURITY DEFINER RPC — service role 키 런타임 배제 유지 | "RLS 기본 deny + service role 미사용" 원칙과 프록시의 쓰기 필요를 모순 없이 양립 (검토 C-1) | 런타임 service role 허용: 유출 시 전체 데이터 노출 반경. RLS 개방: 클라이언트 카운터 조작 가능 |
| 12 | 2026-08-06 (GATE 1차) | 사진 버킷 = public-read + 무작위 128bit 경로(식별정보 없음), 쓰기만 RLS | 비로그인 공유 뷰·오프라인 캐시·썸네일 프리페치가 서명 URL 만료 없이 성립 (검토 H-4). 신뢰 모델이 공유 토큰과 동일 | 비공개+서명 URL: 익명 공유 뷰어가 URL 생성 불가·1h 만료가 캐시 파괴. 서버 스트리밍 프록시: service role 재도입 + 함수 트래픽 낭비 |
| 13 | 2026-08-06 (GATE 1차) | 인증 = 매직링크 + 6자리 OTP 코드 병행, PWA에서는 코드 입력이 기본 | iOS 설치형 PWA는 Safari와 스토리지 격리 — 링크 탭 시 세션이 Safari에 생기는 함정 (검토 H-7) | 매직링크 단독: 주 사용 채널(PWA)에서 로그인 불능 |
| 14 | 2026-08-06 (GATE 1차) | 백업 = GitHub Actions 일일 pg_dump(30일 보관+월 1회 12개월), RPO 24h | Supabase 무료 플랜 자동 백업 미제공을 공식 문서로 확정 (검토 C-2 — 기존 가정 오류 정정) | Pro 승급($25/월): 무료 티어 원칙 위반. 월 1회 수동 dump: RPO 30일은 여행 기록 소실 허용치 초과 |
| 15 | 2026-08-06 (GATE 1차) | Day 내 Stop·Leg = 통합 position 시퀀스, 시각은 표시 정보(정렬 키 아님) | 무시각 항목과 시각 항목의 상호 순서가 데이터로 표현 가능해짐 — 타임라인 결정성 (검토 H-9) | 시각 우선 정렬+별도 position: 병합 결과 비결정적. 단일 day_items 테이블: Stop·Leg 필드 이질성으로 스키마 오염 |
| 16 | 2026-08-06 (GATE 1차) | provider enum = naver·kakao·google·manual (link nullable) | 카카오 폴백(RB-1)·좌표 직접 등록(FR-016)이 계약 위반 데이터를 만들지 않게 (검토 M-2·H-6) | naver·google 2값: 폴백·수동 등록이 스키마 밖 존재가 됨 |
| 17 | 2026-08-06 (사용자 피드백) | Leg에 가격 필드(`cost_amount` 원 단위 정수) + Day 지출 합계 표시. 정산 계산·분배는 여전히 2단계 | "출발·도착 시간이나 가격 같은 거 넣어서 스케줄이나 나중에 정산할 수 있도록" — 데이터를 지금부터 기록해야 나중에 정산이 가능 | 정산 기능 전체 구현: MVP 범위 초과. 금액 미기록: 나중에 소급 입력 불가능(영수증 유실) |
| 18 | 2026-08-06 (사용자 피드백) | 사진 첨부 대상을 Place + Leg로 확장 (photos.place_id/leg_id 배타, 동일 리사이즈 파이프라인) | 예매 확인·티켓 캡처를 이동 기록에 붙이는 것이 "예매 일정 정보 한눈에" 비전과 직결 | 별도 leg_photos 테이블: 파이프라인 중복. Leg 사진 미지원: 예매 캡처를 메모에 못 담음 |
| 19 | 2026-08-06 (T0 실측) | 검색 API 발급처 = **NCP NAVER API HUB** (`/search/v1/local`, X-NCP-APIGW 키쌍). 검색 결과 5건 고정, 좌표 판별기는 3형식(KATECH·WGS84e7·WGS84 도) 지원 | 개발자센터 신규 등록에 검색 API가 없음을 사용자가 실측 — API HUB 공식 문서로 이관 확인. 콘솔 신청 화면에서 지역 검색 일 25,000회 무료 제공도 사용자 확인 완료 | 개발자센터 경로 고수: 신규 발급 불가라 대안 없음. 프록시 일 12,500 상한은 무료 제공량의 50% 과금 방어선 |
| 20 | 2026-08-06 (T0-4 실호출) | 지역 검색 base URL = `https://naverapihub.apigw.ntruss.com`, endpoint = `GET /search/v1/local`, `display=5`. `mapx/mapy` 실측 형식 = WGS84 × 10^7 정수 (`1263658809`, `348019423` 형태) | `.env`의 NCP NAVER API HUB 키쌍으로 `목포 맛집` 검색 실호출 성공. 응답 필드: `title, link, category, description, telephone, address, roadAddress, mapx, mapy`; HTML 태그 제거 필요성도 재확인 | 기존 후보 base URL `https://naveropenapi.apigw.ntruss.com`는 404. KATECH 고정 변환은 이번 API HUB 응답에는 부적합하므로 기존 3형식 판별기를 유지 |
| 21 | 2026-08-07 (T3 검토 반영) | ① stops의 `(day_id, place_id)` 유니크 제거 — 같은 장소를 하루 2회 배치 허용 ② RLS GRANT 최소화 — anon·운영 테이블 잔여 grant 삭제(접근 시 permission denied) ③ 썸네일 320px `prepareTripPhoto` 추가 ④ pgTAP 매트릭스 22어서션 확장 | ①은 계약에 없던 제품 제약(카페 재방문 등 실사용 차단), ②는 05 권한 모델("share-viewer 테이블 접근 0·운영 테이블 전면 deny")과 문자 그대로 일치시킴, ③은 SPEC(SC-002 프리페치 전제) 누락 보완, ④는 T3-2 완료 기준 충족 | 유니크 유지: 재방문 차단을 정당화할 요구 없음. grant 유지: 정책 실수 시 즉시 노출되는 장전된 총 |
| 22 | 2026-08-07 (T3-3 구현 중 발견) | `get_shared_trip` 실패 카운터(share_fail)는 예외와 함께 롤백됨 — PostgreSQL에 autonomous transaction이 없어 RPC 내부 계측(04 알람 #4)이 트랜잭션 의미론과 충돌. 공유 뷰(P2) 구현 시 "ShareView가 예외 포착 후 별도 트랜잭션에서 카운터 RPC 호출" 방식으로 이행 | 공유 기능 자체가 P2라 지금은 문서화만 — 함수 주석에도 명시됨 | dblink·백그라운드 워커: 개인 도구에 과잉. 계측 포기: 알람 #4(토큰 대입 감지) 무력화 |
| 24 | 2026-08-07 (사용자 피드백) | 지출 기록을 방문(Stop)으로 확장 — `stops.cost_amount`(원 단위 정수, 선택 입력) 추가, Day 지출 합계 = Stop+Leg 합산 | "맛집에도 가격 같은 정보 저장해서 정산할 때 쓸 수 있는 구조" — 장소(Place)가 아닌 Stop에 붙이는 이유: 같은 장소 2회 방문 시 지출이 각각이고, 정산은 실제 방문에 귀속됨(#17과 동일 원칙). 정산 계산·분배 기능 자체는 여전히 2단계 | places.cost_amount: 재방문 지출 표현 불가·보관함 단계의 가격은 확정 전 추정치라 memo가 적절 |
| 23 | 2026-08-07 (T5 구현 중 발견) | ① category 실측 정정 — API HUB의 최상위 토큰은 요리명("한식>칼국수,만두"): 힌트 어휘를 요리 분류로 확대 ② Problem type 3종 신설(auth/required·validation/query-too-short·search/unavailable) ③ 설계 공백: 502 시 `cached[]`는 5분 내 캐시만 가능(get_cached_search가 5분 필터) — stale 제공은 `get_stale_search` RPC 추가 필요(T5-3 백로그) | ①은 실측 데이터(T0-4·스모크)가 SPEC §알고리즘 3의 전제와 다름을 증명 ②는 05에 없던 상태의 계약 공백 보완 ③은 "장애 시 오래된 결과라도"의 실행 경로 부재 발견 | ① 퍼지 매칭(식·요리 포함 여부): 오탐 위험, 힌트는 명시 어휘가 안전 ③ 즉시 RPC 추가: T6 차단 안 하는 백로그로 |
| 25 | 2026-08-11 (T0-2 실브라우저 검증) | NCP Maps(Web Dynamic Map) 실키가 `localhost:3010`에서 **인증 성공** — 실지도 렌더 확인. **08 잔여우려 1(카카오 폴백) 해소**: 폴백 착수하지 않는다 | 정찰 스펙으로 로그인→시드 여행→캔버스 진입 후 6초 대기(인증 실패는 mount 이후 비동기로 오므로) 판정: `naver.maps` 네임스페이스 존재·타일 이미지 56장·fake 폴백 배너 0건·인증 실패 배너 0건, 스크린샷 육안 확인(제주 지도+핀). 무료 이용량은 공식 정책상 Web Dynamic Map 월 600만 건이 **대표 계정**(Maps API를 처음 이용한 계정)에 제공 — 개인 사용량과 자릿수 차이가 커 과금 위험 낮음 | 카카오 폴백(RB-1) 착수: 실키 인증이 확인되어 불필요. 단 `provider` enum의 `kakao` 값(#16)과 `MapProvider` 추상화(#2)는 그대로 둔다 — 장래 폴백 여지를 없앨 이유가 없다 |
| 26 | 2026-08-11 (T5-3 구현) | 502 캐시 폴백을 `get_stale_search(qhash)`(새 RPC, 마이그레이션 `0006`)로 분리 — 5분 창을 무시하되 **상한 7일**. 정상 경로의 `get_cached_search` 5분 판정은 그대로 둔다 | 구현 중 발견: `upstreamProblem`이 `get_cached_search`를 다시 불렀는데, 같은 요청이 직전에 이미 그 함수로 miss를 낸 뒤라 **`cached[]` 분기가 도달 불가능한 죽은 코드**였다(기존 테스트는 실패하는 fetch 안에서 캐시를 채워 5분 이내로 만들어 이 사실을 가렸다). 두 조회는 묻는 것이 다르다 — 정상 경로는 신선도를, 폴백은 존재 여부를 묻는다. 상한을 둔 이유는 `search_cache`에 파기 주기가 없어 행이 영구히 남기 때문 — 무제한이면 몇 달 전 결과를 "검색 결과"로 내주게 된다. 문구도 "방금 받아둔"→"전에 받아둔"으로 정정(사실과 어긋남) | 같은 함수에 `max_age` 인자 추가: 호출부가 의도를 잃고 정상 경로에서 실수로 stale을 요청할 여지. 상한 없는 stale: 폐업·이전한 장소를 무기한 제공. 프록시에서 5분 판정을 걷어내기: 정상 경로의 중복 과금 방어선이 무너진다 |
| 27 | 2026-08-11 (사용자 피드백) | 새 여행 생성에서 **이름·날짜를 묻지 않는다** — "새 여행 만들기"가 곧 초안 생성(이름 '제목 없는 여행'·기간 오늘 하루)이고 바로 캔버스로 보낸다. 이름은 헤더의 `TripTitleField`, 기간은 그 옆 `DateRangeCalendar`(범위 선택 달력, 지난 날짜 비활성, 상한 없음)에서 고친다. 스키마는 그대로 둔다(start/end NOT NULL 유지) | "시작페이지도 날짜 설정하는거 굳이 이런 단계가 필요할까? 지도 페이지 우상단 같은곳에 저장할 수 있게" — 여행 계획은 "언제 갈지"보다 "어디 갈지"에서 시작하는 일이 많고, 기간 고치기 UI(FR-015)가 이미 헤더에 있어 나중에 고치는 경로가 이미 존재했다. 폼 단계가 사라지며 SC-001 계측도 1106ms→647~819ms 로 짧아졌다 | 스키마를 날짜 nullable 로: 마이그레이션 + days 지연 생성 + 일차 탭·타임라인 전반 수정이 필요한데, 기본값 하루로도 같은 사용자 가치를 얻는다. 이름만 받는 폼 유지: 물어볼 이유가 여전히 없다(빈 이름도 헤더에서 고칠 수 있다) |
| 28 | 2026-08-11 (울트라코드 리뷰에서 발견) | 달력 날짜 버튼에서 **mousedown 은 선택을 바꾸지 않는다** — "끌기 후보"만 기록하고 다른 날로 넘어갈 때(mouseenter)만 끌기로 승격한다. 끌기 뒤 따라오는 click 은 삼킨다 | 같은 버튼에 onClick·onMouseDown·onMouseUp 을 함께 걸었더니, 실제 포인터 순서(mousedown→mouseup→click)에서 두 번째 날의 mousedown 이 첫 앵커를 덮고 mouseup 이 앵커를 비워, **두 번 눌러 범위 잡기가 항상 하루짜리로 무너졌다**. 터치는 탭도 호환 마우스 이벤트를 발사하고 mouseenter 는 오지 않아 **모바일에서 여러 날 여행을 아예 만들 수 없었다**(주 타깃 390×844). 단위 테스트가 `fireEvent.click` 만 써서 322개 통과가 이 결함을 가렸다 — 실제 이벤트 순서(mousedown→mouseup→click) 회귀 테스트 2건과 E2E 기간 반영 단언을 함께 추가했다 | Pointer Events + 이동 임계값: 지금 필요보다 복잡. click 만 남기고 끌기 제거: 데스크톱 편의를 잃는다 |
| 29 | 2026-08-11 (사용자 피드백) | 지도에서 직접 찍는 경로에 **보이는 문**을 만든다 — 리스트 위 "지도에서 찍기" 버튼이 모드를 켜고, 켜진 동안의 지도 탭 한 번이 미니 폼을 연다(`MapProvider.onMapTap` 신설). 롱프레스·우클릭은 그대로 남긴다 | "검색을 통해서만 추가하는게 아니라 실제 위치 클릭해서 지정할수 있으면" — 기능(FR-016)은 T6-5에 이미 있었으나 진입점이 롱프레스·우클릭뿐이라 발견 불가였고, 안내 문구도 검색 0건일 때만 떴다. 그냥 좌클릭에 바로 걸지 않은 이유: 지도 이동·핀 선택과 부딪힌다 — 모드를 켠 화면만 탭을 소비한다. 버튼에 강조색을 쓰지 않은 이유: 이 화면의 주 행동은 여전히 검색 입력이다 (L-09) | 좌클릭 상시 수신: 지도를 못 쓰게 된다. 안내 문구만 상시 노출: 읽어도 롱프레스를 해야 해 터치 발견성이 그대로다 |
