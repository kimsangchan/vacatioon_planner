<!-- NEXT-ACTION:START -->
## ▶ 지금 할 일 (새 세션은 이 블록부터 — SessionStart 훅이 자동 주입)

**MVP 배포됨**: https://vacatioon-planner.vercel.app
검증 기준선: vitest **536**(`--no-file-parallelism`) · pgTAP **209** · **E2E 4 통과** · tsc 0 · lint 0
(2026-08-20 실측)

### 운영에 반영됨 (2026-08-20) — 이번 세션에 들어온 것

1. **길찾기를 NCP Directions 15 로** (결정 #45). 카카오로 짰다가 되돌렸다 — 근거가 틀렸다
2. **확정/미확정 + 확정된 것만 경로** (결정 #47, `0011`). 기본값 true
3. **별표 협의** (결정 #46, `0010`·`0012`) + **공유 뷰 `/s/[token]`**.
   계정 없이 링크만으로 별을 남긴다. 실브라우저로 왕복 확인
4. **TDS(토스) 디자인** (결정 #48). 토큰·Pretendard·탭·버튼 사다리·list-row·아이콘
5. 하단탭 자동 숨김 · PC 좌측 패널 여닫이 · PC 는 장소 상세가 패널 안으로
6. **지도에서 순서·경로 읽기** (결정 #49). 핀 숫자 = 그 날 몇 번째 방문 · 일차 색 경로선

### 다음

- **[버그] T10-7** 로그인 직후 홈이 500 — `JWT issued at future`. 컨테이너 시계 1초 차로
  `getUser()` 는 통과하고 PostgREST 만 거부해 결정 #31 의 그물에 안 걸린다. L-06 위반
- T10-5b 도보 이동시간 · T10-6 검색 5건 상한(카카오 Local 전환)

### 확인이 안 끝난 것

- **모바일(390×844) 육안 확인은 아직이다.** E2E(SC-004)가 390×844 로 돌아 통과하지만,
  하단탭 자동 숨김·핀에 붙는 카드를 눈으로 본 것은 아니다
- `fuelPrice`(유류비)를 응답으로 받아만 두고 화면엔 안 쓴다 — 경비 요약(#39)에 붙일지 정할 것
- **통합 테스트는 병렬로 돌리면 산발적으로 깨진다** (2026-08-20 원인 확정) —
  `npx vitest run --no-file-parallelism` 이면 529/529. 로컬 Supabase 하나를 두고 경합한다

### 사용자 몫으로 남은 것

- **실기기(아이폰)에서 배포본 육안 확인** — 특히 이동시간·경로선. 배포본에서 실제로 값이 나오는지는
  로그인해야 알 수 있어 아직 확인 못 했다(스모크는 미인증 401 까지만)
- GitHub Secrets 에 `SUPABASE_URL`·`SUPABASE_ANON_KEY` 등록 → keepalive 가동

### 함정은 그 폴더의 CLAUDE.md 에 있다

`src/components/CLAUDE.md`(jsdom 레이아웃·지도 투영·`h-full`·포인터 캡처·id 중복) ·
`supabase/CLAUDE.md`(`db reset` 데이터 삭제·테스트 경합) ·
`e2e/CLAUDE.md`(오래 뜬 dev 서버·`.next`) · `AGENTS.md`(포트 3010·build·lockfile)

**이번에 또 겪은 것**: dev 서버를 켜 둔 채 vitest 를 돌리면 SC-008 쿼터 테스트가 로컬 Supabase 를
두고 경합해 깨진다. 코드 문제가 아니다 — dev 를 내리고 다시 돌리면 524/524 다.
<!-- NEXT-ACTION:END -->

<!--
규칙:
- 이 마커 사이는 "지금/다음 할 일"만. 완료분은 WORKLOG.md 의 ## History 로.
- 전체 작업 목록의 원본은 tasks.md, 결정 근거는 docs/design/decision-log.md (#45까지).
- **근거를 여기에만 적지 마라.** 이 블록은 개정 때 통째로 갈린다 — "왜 길찾기만 카카오냐"가 여기
  적혔다가 사라졌고, 되찾고 보니 **그 근거 자체가 틀렸다**(2026-08-20 콘솔 실측). 근거는 decision-log 로,
  그리고 **검증한 날짜와 방법을 함께** 적어라.
- 훅(tools/hooks/print_next_action.py)은 이 마커 사이만 세션에 주입한다.
-->
